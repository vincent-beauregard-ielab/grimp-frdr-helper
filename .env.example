# Copy this file to .env and adjust values as needed.
# .env is gitignored — never commit real tokens.

# Jupyter server token — must match the value in .claude/settings.json and .vscode/mcp.json
JUPYTER_TOKEN=grimp-local-token

# Jupyter server port (default: 8888)
JUPYTER_PORT=8888
